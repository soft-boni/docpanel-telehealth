
  # Setup Dashboard Routing Structure

  This is a code bundle for Setup Dashboard Routing Structure. The original project is available at https://www.figma.com/design/q6UT6FS7IM3oy5XufiOPea/Setup-Dashboard-Routing-Structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  